[project]
name = "claude-canvas-wandb-test"
version = "0.1.0"
description = "Test utilities for Claude Canvas W&B integration"
requires-python = ">=3.9"
dependencies = [
    "wandb>=0.15.0",
]

[project.scripts]
test-wandb-run = "test-wandb-run:main"
